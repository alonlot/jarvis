"""Drop plugin modules in this directory. Each module that defines functions
decorated with `@tool(...)` will be auto-discovered at startup.

Replace the *_stub modules below with real implementations of your APIs.
"""
